export type UserInfo = {
	items: number,
	score: number,
	rank: number,
}

export type User = {
	id: string,
	name: string,
}

export type UserLeaderboard = {
	name: string;
	cnt_items: number;
	score: number;
};
